# Update your .env file with Pusher configuration

# Set BROADCAST_DRIVER to pusher
BROADCAST_DRIVER=pusher

# Add Pusher configuration (get these from your Pusher dashboard)
PUSHER_APP_ID=your_pusher_app_id
PUSHER_APP_KEY=your_pusher_key
PUSHER_APP_SECRET=your_pusher_secret
PUSHER_APP_CLUSTER=your_pusher_cluster

# Optional: Add Pusher host and port if using local Pusher instance
# PUSHER_HOST=127.0.0.1
# PUSHER_PORT=6001
# PUSHER_SCHEME=http
